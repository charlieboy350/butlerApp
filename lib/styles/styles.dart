import 'package:flutter/material.dart';

class Styles {
  /// padding use by all over the application
  static EdgeInsets padding8 = EdgeInsets.all(8.0);
  
  static RoundedRectangleBorder shapeBorderCircular5 = RoundedRectangleBorder(
    borderRadius: BorderRadius.circular(5.0),
  );
}
